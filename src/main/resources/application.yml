server:
  port: 8080

spring:
  datasource:
    url: jdbc:mariadb://10.1.1.1:3306/smartKitchen
    username: agonylua
    password: wei.liu-liu
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true
        use_sql_comments: true
        jdbc:
          batch_size: 20

mqtt:
  broker: tcp://10.1.1.1:1883
  username: smartKitchen
  password: wei.liu-liu
  client-id: spring-mqtt
  default-topic: test/topic
  subscribe-topics: device/+/report,device/+/status,C/B/#,A/B/#

logging:
  level:
    org.springframework.integration: DEBUG